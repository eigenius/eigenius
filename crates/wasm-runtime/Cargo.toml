[package]
name = "eigenius-wasm-runtime"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Shared wasmtime runtime primitives for Eigenius kernel and orchestrator WASM hosting."

[dependencies]
wasmtime = { workspace = true }
