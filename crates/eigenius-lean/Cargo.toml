[package]
name = "eigenius-lean"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
build = "build.rs"
description = "Lean 4 verification institution for Eigenius. Wraps nanoda_lib as an in-process proof checker; the kernel binary registers a LeanInstitution that dispatches proof_check verdicts through this crate. See D28, Phase 20a."

[dependencies]
# Lean term checker. Read the export directly and call
# `ExportFile::check_all_declars` in-process per D28 §2.3 (no IPC,
# verdict is a function call). Catching its panics with
# `std::panic::catch_unwind` is the documented contract for v1.
#
# Pinned to a commit SHA rather than a tag because upstream's last
# published tag (v0.3.2) predates the `Option<Vec<String>>` shape on
# `Config::permitted_axioms` / `pp_declars` and the
# `unknown_pp_declar_hard_error` field that `check_proof` relies on.
#
# `2026-09-03`: repointed at `eigenius/nanoda_lib`, a fork of upstream carrying ONE commit —
# `TypeChecker::ctx`, an accessor letting a caller build expressions while a checker is alive.
# D74 §4.6: from outside the crate, inference and construction are mutually exclusive
# (`TypeChecker::new` asserts `dbj_level_counter == 0`, and the `ctx` field is `pub(crate)`), so
# externalizing a `Fst`/`Snd` under a binder — which needs the scrutinee's type inferred while the
# binder is open — is impossible without it. The branch is off upstream `master`, so this also
# takes their "prohibit orphan recursors" tightening, which our previous pin `6ae1f0c` predates.
#
# Upstreaming is the intent; the fork exists so the work is not blocked on their review.
nanoda_lib = { git = "https://github.com/eigenius/nanoda_lib", rev = "1e44c4964b5ec916a1508c365af5f0e8a8c736fe" }

# Kernel dep wired in 20a.4: `LeanInstitution` implements
# `kernel::institution::Institution`.
eigenius-kernel = { path = "../../kernel" }

# D74 §3.3: the IRI -> Lean `Name` mangling is a SINGLE authority, and it lives with D30's
# emitter (`mirror_gen::lean_name`). The externalizer must spell a constant exactly as the
# generator did or the comparison fails on naming rather than on content, so it calls the same
# function rather than reimplementing it. Promoted from a dev-dependency for this.
eigenius-lean-runtime = { path = "../eigenius-lean-runtime" }

# D74 §4 — `LitInt` maps to Lean's `NatLit`, whose payload is a `BigUint`. Same version nanoda
# pins, so the type crossing `mk_nat_lit_quick` is the one it expects.
num-bigint = "0.4.4"

thiserror.workspace = true
serde_json.workspace = true

# D28 §5.5 anchor-consistency check recomputes the
# `library_content_hash` over a mirror's embedded archive and
# compares to the declared value. Matches the substrate-side
# digest in `eigenius-lean-runtime::mirror_gen::library_content_hash`.
sha2 = "0.10"

# `check_proof` takes `&[u8]` but nanoda's `Config::to_export_file`
# only accepts a `PathBuf`. Until upstream takes a `Reader`, we stage
# the bytes into a tempfile per call — that hop lives in the public
# API path, so the dep belongs in `[dependencies]`, not dev-deps.
tempfile = "3"

[dev-dependencies]
# `eigenius-lean-runtime` is a real dependency now (see above) — the verification side calls
# D30's naming authority so it spells a constant the way the generator did. It was dev-only
# while only `examples/gen_verification_demo.rs` needed it, for `mirror_to_resource`.
# `MirrorGenerationOutput::library` carries a `LibraryContent::Embedded`
# value, whose entries are `LibraryFile { path, content }`. The
# generator binary stages the capstone Lake project into one.
eigenius-runtime-substrate = { path = "../runtime-substrate" }
